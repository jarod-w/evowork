---
description: 新功能该落在哪一层、Q1–Q29 里会改变写码方式的决策。添加功能或讨论产品范围时使用。
alwaysApply: false
---

# 落层与已定决策

先走 `CLAUDE.md` §4 的决策树，再写代码。完整表格在总纲 §10.1。

| 决策 | 写码时意味着什么 |
|---|---|
| Q1 = 纯本地桌面 | 调度 / 解析 / 产物索引是本机常驻，不是云服务 |
| Q2 / Q16 | 网关按全量适配；P0 是 DeepSeek · Kimi · GLM-5.3-flash |
| Q3 / K6 | 没有云端解析兜底 |
| Q9 | connectors 只做 `browser/` |
| Q10 | 分享显式授权、默认关闭、一次一个产物 |
| Q11 | 用 `ThreadGoal.budget` 与 `subagent_start` hook，别自建并发预算 |
| Q14 | 网关 / 日志 / APM / 错误上报不落盘正文 |
| Q15 | automation 绑创建它的设备；迁移重置 misfire 基准 |
| Q17 | 不做个人云盘；资料库显示本机磁盘占用 |
| Q18 | 运营位默认关、禁行为回传；不做积分 |
| Q19 | 团队空间只读订阅 |
| Q20 | 助理 = 一个常驻特殊 Thread，固定 cwd `~/.evowork/assistant/`，别自建会话存储 |
| Q23 / Q24 | Electron + React + 32 自建组件 |
| Q25 | 代码与文档统一 EvoWork |
| Q26 | macOS 首发；Windows 隔离不足时标 `allowed:false` 并给原因页，不静默降级 |
| Q27 | 前端不得直连实验方法 |
| Q29 | 不为自建推理排期；网关保留私有 endpoint 配置项即可 |

`services/identity` **尚未开始**。云端只有 D9 的四件事；不要给它加内容数据面。
