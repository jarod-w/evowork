---
description: 本机定时调度。补偿落两条记录、DST 按本地日历日、迁移重置 misfire 基准。
globs: services/scheduler/**/*
alwaysApply: false
---

# services/scheduler

跑在用户的笔记本上，合盖就会错过。Q8：SKIP + 不自动重试 + 连败 3 次 PAUSE。
自然语言触发解析**不调模型**。

## 四条不能松

1. **补偿落两条记录**：一条 `MISSED / MACHINE_OFFLINE` 指向原定时刻，一条补跑。
   只留补跑，用户永远不知道自己漏了。幂等键是 `automation_id + fire_time + trigger`，
   没有 `trigger` 时补跑插不进去（已回写总纲 §6.9）。
2. **环境原因失败不计入连败，也不清零。**
3. **迁移重置 misfire 基准**（Q15）。不重置，新设备一上线就补一堆历史触发。
4. **时区显式存储。** 不依赖运行时系统时区。

## DST（`cron.ts`）

按**本地日历日**迭代，每个「日+时+分」只产生一个候选。不要改回 UTC 游标：

- 春季跳过（02:00→03:00）：在跳变后的第一个瞬间触发（03:00），不可少跑一天
- 秋季重复：只触发一次

这两条各有测试。错了要等半年才有人报。
