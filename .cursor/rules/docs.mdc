---
description: 文档与 STATUS 的编号纪律
globs: docs/**
alwaysApply: false
---

# docs

`docs/` 保持中文。commit message 仍是英文。

`docs/STATUS.md` 是当前真实状态与未做事项。写它的前提是：「结构写好了」
不等于「接通了」，「测试绿了」不等于「检查有效」。每一条未做项要写
**它今天会怎么坏**，而不只是「还没做」。

- 条目编号（1–16）按新增顺序追加，**不随优先级重排**。重排会让所有既有
  引用失效。新条加在对应优先级节的末尾。
- 第三节「UI 优先」只改顺序，不改第四节的优先级判据、条目编号与措辞。
- 不要把注释或设计文档里「本层保证 X」写进 STATUS 的「已完成」，除非代码
  里有读者、有写入方、有一条会在未接通时变红的测试。

设计文档以 `docs/design/00-index.md` 为索引。改契约时改对应的 01–09，
不要只改散文。待确认问题的编号（Q-xx）同样只追加、不重排。

过强的声明比没有声明更危险。M2 终审在一条分支上数出十处「注释断言了
一件代码不做的事」。文档同样适用。
