---
description: 产物索引、分享授权、资料库删除语义。文件系统是真源。
globs: services/artifacts/**/*
alwaysApply: false
---

# services/artifacts

D6：文件系统是真源，本包只存索引。识别 100% 自建（内核那套只喂 analytics、没有 chart）。

信号优先级是正确性要求，不是排序偏好：**技能上报 > FileChange > hook**。
png 是 chart 还是 image 只有信号源知道。内容哈希没变但来了更高优先级信号时走
`corrected` 元数据订正，不要当重复丢掉。

## 分享（本机内容离开设备的唯一常规出网路径）

- 一次一个 `artifactId`（类型约束，不是文档约定）。
- 勾选框不预勾。不做「记住授权」、不做批量。

## 两种删除是两个函数

| 位置 | 语义 | 磁盘 |
|---|---|---|
| 我的资料 | 真删文件 | 删掉，不进回收站 |
| 本地产物 | 只删索引 | 保留（可选同时删，默认不勾） |

不要合成一个带布尔参数的函数。Q17：配额条显示本机磁盘占用，动作是「清理」不是「升级」。
日志字段名必须用注册表里已有的（`byteSize` 不是 `sizeBytes`）——未注册的会被静默丢掉。
