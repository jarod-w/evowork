// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BlobRef } from "./BlobRef";
import type { PlanIntent } from "./PlanIntent";
import type { PlannedCall } from "./PlannedCall";
import type { PlannedClarification } from "./PlannedClarification";
import type { TaintLevel } from "./TaintLevel";

/**
 * runtime 从 model.responded 解析出的结构化决策。内核只吃这个，不碰模型原文。
 *
 * `call` 是对 01 §4.3 的新增 optional 字段：内核要发 RequestEffect，
 * 必须从这里拿到工具名与参数引用（class / targets 由 Gateway 从 manifest 补全）。
 *
 * `clarification` 与 `call` 对称：`AskClarification` 分支要问的问题正文
 * 与选项，同样必须从这里拿，不能靠 daemon 拿着 `response_ref` 再对模型
 * 原文 `parse_plan` 一遍——那是两处消费同一份解析结果、只靠人记得保持
 * 同步的脆弱模式。`call_model` 已经 `parse_plan` 过一次，这里把那次解
 * 析的产物直接落盘。
 */
export type PlanStep = { turn: number, intent: PlanIntent, rationale_ref?: BlobRef | null, taint_inherited: TaintLevel, call?: PlannedCall | null, clarification?: PlannedClarification | null, };
