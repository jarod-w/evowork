// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * 触发器怎么醒。这是 RPC 载荷，不是 Run Log 事件——跑起来的那一条
 * run 只在 `run.created.trigger` 里留 kind + reference，定义本身活在
 * daemon 的 `triggers.sqlite`。
 *
 * `file` / `condition` 在 [`TriggerKind`] 上有位，create 拒绝它们：A-8
 * 只接通定时与一个 webhook，没有假装已经在听文件或指标。
 */
export type TriggerSpec = { "kind": "once", at_ms: number, } | { "kind": "interval", every_ms: number, } | { "kind": "daily", hour: number, minute: number, tz_offset_minutes: number, } | { "kind": "weekly", 
/**
 * 0 = Monday … 6 = Sunday.
 */
weekday: number, hour: number, minute: number, tz_offset_minutes: number, } | { "kind": "monthly", 
/**
 * 1–31. Months that do not have this day are skipped (31 in
 * February → March 31), not clamped.
 */
day: number, hour: number, minute: number, tz_offset_minutes: number, } | { "kind": "webhook" };
