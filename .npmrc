# 依赖必须显式声明：幽灵依赖在 Electron 打包（M9）时才暴露，代价太高
shamefully-hoist=false
strict-peer-dependencies=false
# Electron 的二进制下载在 CI 里按需开启，本机开发默认跳过（M9 才需要真二进制）
# ELECTRON_SKIP_BINARY_DOWNLOAD 由 CI/开发者显式设置，不写死在这里
auto-install-peers=true
